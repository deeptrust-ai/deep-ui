// Re-export Frosted UI components for convenience
import { Theme as FrotedTheme, Text as FrostedText } from 'frosted-ui';

const Theme = FrotedTheme;
const Text = FrostedText;

export { Theme, Text };

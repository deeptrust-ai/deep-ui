import type { Meta, StoryObj } from '@storybook/react-vite';
import { {{pascalCase name}}{{#if useTypes}}, type I{{pascalCase name}}Props{{/if}} } from '../..';

const meta = {
  title: '{{pascalCase componentType}}/{{pascalCase name}}',
  component: {{pascalCase name}},
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
  args: {},
} satisfies Meta<{{#if useTypes}}I{{pascalCase name}}Props{{else}}typeof {{pascalCase name}}{{/if}}>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};
